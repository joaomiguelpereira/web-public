# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

#Routes for Offices
GET /offices                                    Offices.index
GET /offices/pre-register                       Offices.preRegister
GET /myoffices									Offices.listUserOffices
GET /myoffices/{id}								Offices.view

#Users

GET /user/{userType}/new						Users.blank
POST /user/{userType}				     		Users.save
GET /users							     		Users.index
GET /user/registrationConfirmation				Users.userCreatedConfirmation
GET /user/activate/{activationKey}				Users.activateUser
GET /login										Users.login




# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
